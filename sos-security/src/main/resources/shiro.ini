[main]
#ldapRealm1
ldapRealm = com.sos.auth.shiro.SOSLdapAuthorizingRealm
ldapRealm.userDnTemplate = cn={0},ou=ehp,cn=Manager,dc=my-domain,dc=com
ldapRealm.searchBase = ou=ehp,cn=Manager,dc=my-domain,dc=com
ldapRealm.contextFactory.url = ldap://ehp:389
ldapRealm.groupNameAttribute=cn
ldapRealm.userNameAttribute=sn
ldapRealm.groupRolesMap = "SOS01":"admin|jid"
#ldapRealm.permissions = joe:jobscheduler|dashboard|joe,joe:jobscheduler|dashboard|joe

rolePermissionResolver = com.sos.auth.shiro.SOSPermissionResolverAdapter
rolePermissionResolver.ini = $iniRealm
ldapRealm.rolePermissionResolver = $rolePermissionResolver


securityManager.realms = $ldapRealm
cacheManager = org.apache.shiro.cache.MemoryConstrainedCacheManager
securityManager.cacheManager = $cacheManager


[main_]
sosRealm = com.sos.auth.shiroSOSHibernateAuthorizingRealm
sosRealm.hibernateConfigurationFile = c:/temp/hibernate.cfg.xml
securityManager.realms = $sosRealm
cacheManager = org.apache.shiro.cache.MemoryConstrainedCacheManager
securityManager.cacheManager = $cacheManager

[users]
root = secret, jobscheduler_dashboard_admin
ur = ur, joc, joe
joe = joe, joe
guest = guest

[roles]

admin = jobscheduler:jid:*,jobscheduler:joe:*,jobscheduler:joc:*
jobscheduler_dashboard_admin = jobscheduler:jid:*
jid = jobscheduler:jid:*
joc = jobscheduler:dashboard:joc
joc_admin = jobscheduler:dashboard:joc:*
joe = jobscheduler:dashboard:joe
events = jobscheduler:dashboard:events
jobnet = jobscheduler:dashboard:jobnet

Map property value [joe=*] contained key-value pair token [joe=*] that does not properly split to a single key and pair.  This must be the case for all map entries.