<?xml version="1.0" encoding="iso-8859-1"?>
<!-- $Id$ -->

<param xmlns="http://www.sos-berlin.com/schema/scheduler_job_documentation_v1.1"
       xmlns:xhtml="http://www.w3.org/1999/xhtml"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.sos-berlin.com/schema/scheduler_job_documentation_v1.1
     ../scheduler_job_documentation_v1.1.xsd "

       name="atomic_prefix"
       required="false"
       DataType="SOSOptionString"
       see="atomic_suffix, transactional"
       constraint="append_files"
       Category="Filehandling, atomic, filewatcher"
	   Alias="TargetTransferFilenamePrefix"
       >

	       <indexterm index="atomic_prefix, parameter" display="false"></indexterm>
    <indexterm index="parameter, atomic_prefix" display="false"></indexterm>
 
        <note language="de">
        <title>Präfix für den Namen der Target Transfer Datei</title>
          <div xmlns="http://www.w3.org/1999/xhtml">
            <p>
            Der Parameter bestimmt, ob der Name der Zieldatei (Target-Transfer-Dateiname) zunächst mit einem Präfix, 
            z.B. "~", erzeugt wird.
            Nach vollständiger und fehlerfreier Übertragung 
            wird dieser Name dann in den endgültigen Zieldateinamen (Target-Dateiname) geändert.
            Dies stellt sicher, dass die Datei "atomar" im Ziel (-Verzeichnis) angelegt wird.
            </p>
         <p>
            Als Wert des Parameters ist eine Zeichenfolge (mit mindestens einem Zeichen) für den Präfix anzugeben.
         </p>
         <p>
            Die Verwendung des Präfix wird empfohlen, wenn das Zielverzeichniss von einer Applikation oder
            vom JobScheduler (für Dateiaufträge) überwacht wird.
            Insbesondere bei der Übertragung großer Dateien wird dadurch vermieden, 
            daß eine Dateiüberwachung noch während der Übertragung die
            Verarbeitung der Datei beginnt.
         </p>
          </div>
        </note>
        <note language="en">
        <title>Prefix for the target transfer file name</title>
          <div xmlns="http://www.w3.org/1999/xhtml">
		  <p>
            This parameter specifies whether target files should be created with a prefix such as "~"
            and have to be renamed to the target file name after the file transfer has been completed without errors.
            This mechanism is useful if the target directory is monitored for incoming files
            by an application such as JobScheduler
            and if files are required to appear atomically - i.e during transfer - 
            instead of being shown after transfer has been completed.
            </p>
         <p>The value of this parameter is the temporary prefix.
			</p>
            <p></p>
          </div>
        </note>
      </param>