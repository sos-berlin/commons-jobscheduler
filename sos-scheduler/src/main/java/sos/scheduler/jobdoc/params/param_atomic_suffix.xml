<?xml version="1.0" encoding="iso-8859-1"?>
<!-- $Id$ -->
<param
	xmlns="http://www.sos-berlin.com/schema/scheduler_job_documentation_v1.1"
	xmlns:xhtml="http://www.w3.org/1999/xhtml" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.sos-berlin.com/schema/scheduler_job_documentation_v1.1     http://www.sos-berlin.com/schema/scheduler_job_documentation_v1.1.xsd"
	name="atomic_suffix" required="false" DataType="SOSOptionString"
	constraint="append_files" Category="Filehandling, atomic, filewatcher"
	Alias="TargetTransferFilenameSuffix">

	<indexterm index="atomic_suffix, parameter" display="false"></indexterm>
	<indexterm index="parameter, atomic_suffix" display="false"></indexterm>
	<indexterm index="atomic transfer, atomic_suffix" display="false"></indexterm>

	<note language="de">
		<title>Suffix für den Namen der Target Transfer Datei</title>
		<caption>TTF-Name Suffix</caption>
		<abstract>Suffix für den Namen der Target Transfer Datei, wird benutzt
			um atomaren Transfer sicherzustellen
		</abstract>
		<description>
			<p>
				Der Parameter bestimmt, ob die Namen der Zieldateien zunächst mit einem
				Suffix, z.B. "~",
				erzeugt
				und nach vollständiger Übertragung in den endgültigen Zieldateinamen
				umbenannt werden,
				damit die Dateien atomar im Verzeichnis erscheinen.
			</p>
			<p>
				Als Wert des Parameters ist das temporäre Suffix anzugeben.
			</p>
			<p>
				Diese Einstellung wird empfohlen, falls das Zielverzeichniss von einer
				Applikation oder
				vom JobScheduler überwacht wird.
				Damit wird verhindert, daß die Verarbeitung der Datei gestartet wird, 
				bevor die Datei vollständig erstellt und geschlossen wurde.
			</p>
		</description>
	</note>
	<note language="en">
		<title>Suffix for the target transfer file name</title>
		<caption>TTF name suffix</caption>
		<abstract>A Suffix for the target transfer file name, e.g. "~" to
			ensure atomic transfer
		</abstract>
		<description>
		
		<p>
			This parameter specifies whether target files should be created
			with a
			suffix
			such as "~",
			and should be renamed to the target file name
			after the file transfer is
			completed.
			This mechanism is useful if the
			target directory is monitored for
			incoming files
			by some application
			and if files are required to appear atomically instead of being
			subsequently
			written to.
		</p>
		<p>
			The temporary suffix is specified as the value of this parameter.
		</p>
		<p>
			This setting is recommended should target directories be monitored
			by an
			application
			or the JobScheduler.
		</p>
		</description>
	</note>
</param>