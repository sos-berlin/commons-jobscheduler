<?xml version="1.0" encoding="UTF-8"?>
<!--$Id$ -->
<items xmlns="http://www.sos-berlin.com/schema/scheduler_job_documentation_v1.1"
xmlns:xhtml="http://www.w3.org/1999/xhtml"
xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.sos-berlin.com/schema/scheduler_job_documentation_v1.1
    http://www.sos-berlin.com/schema/scheduler_job_documentation_v1.1.xsd"
    >

    <note language="en" >
        <p>
            This job can be used standalone, as a single job, or as an order driven
            job in a jobchain as a jobchain node.
            Parameters are respectively accepted as
            job- or as order-parameters.
        </p>
        <p>
            A job can process multiple parameters that are analysed when the job starts.
            Parameters are defined in the configuration of the job or of the order.
            Parameters can be submitted by API methods, as well.
            Parameters are optional or mandatory and may contain default values.
        </p>
        <p>
             Order parameters have precedence over the same job parameters. Job parameters overrule global JobScheduler parameters.
             <indexterm index="order parameter"></indexterm>
        </p>
    </note>

    <note language="de">
        <p>Dieser Job kann einzeln (eigenständig; stand-alone), außerhalb einer Job-Kette, oder auftragsgesteuert
            als Knoten innerhalb einer Jobkette verwendet
            werden.
            Die Parameter werden entsprechend als Job- oder Auftragsparameter von der Implementierung des Jobs
            interpretiert und verarbeitet.
        </p>
        <p>
            Ein Job kann eine beliebige Anzahl Parameter haben, die vom JobSchedulder dem Job beim Job-Start an den Job übergeben werden.
            Parameter werden in der
            Konfigurationsdatei des Jobs bzw. des Auftrages oder über die entsprechenden API-Methoden definiert und mit Werten versorgt.
            Parameter sind entweder
            obligatorisch oder optional.
            Für einen Parameter kann ein Default-Wert vorgegeben sein. Der gilt dann, wenn der Parameter nicht angegeben wurde. Nicht
            alle Parameter
            haben einen Default-Wert.
        </p>
        <p>
            Auftragsparameter haben Vorrang vor gleichnamigen Job-Parameter. Job-Parameter haben Vorrang vor gleichnamigen globalen JobScheduler Parameter.
             <indexterm index="order parameter"></indexterm>
        </p>
    </note>

</items>