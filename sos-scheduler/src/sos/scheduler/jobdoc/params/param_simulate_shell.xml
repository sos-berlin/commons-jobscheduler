<?xml version="1.0" encoding="iso-8859-1"?>
<param xmlns="http://www.sos-berlin.com/schema/scheduler_job_documentation_v1.1"
       xmlns:xhtml="http://www.w3.org/1999/xhtml"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       name="simulate_shell"
       required="false"
       default_value="false">
        <note language="de">
          <div xmlns="http://www.w3.org/1999/xhtml">
          <p>
            Wird der Wert <em>true</em> für diesen Parameter angegeben, dann wird ein login
            an der shell simuliert, um die Befehle auszuführen. Manche Scripte haben
            Probleme wenn keine shell vorhanden ist.
            <p>The parameter <paramref>simulate_shell_prompt_trigger</paramref> must be specified if this
            parameter is used.
          </p>
            </p>
          </div>
        </note>
        <note language="en">
          <div xmlns="http://www.w3.org/1999/xhtml">
          <p>
            Should the value <em>true</em> be specified for this parameter, 
            then a login to a shell is simulated to execute commands. 
            Some scripts
            may cause problems if no shell is present.
            </p>
            <p>The parameter <paramref>simulate_shell_prompt_trigger</paramref> must be specified if this
            parameter is used.
            </p>
          </div>
        </note>
      </param>