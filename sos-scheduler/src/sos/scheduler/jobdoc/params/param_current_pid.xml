<?xml version="1.0" encoding="iso-8859-1"?>
<param xmlns="http://www.sos-berlin.com/schema/scheduler_job_documentation_v1.1"
    xmlns:xhtml="http://www.w3.org/1999/xhtml" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    name="current_pid" required="false" DataType="SOSOptionProcessID"
>
    <note language="de">
        <title />
        <div xmlns="http://www.w3.org/1999/xhtml">
            Der Parameter wird unter Unix verwendet und - abweichend von anderen
            Parametern -
            i.d.R. bereits im Start Script
            <shell>sosftp.sh</shell>
            eingesetzt.
            Als Wert wird die Umgebungsvariable
            <em>$$</em>
            übergeben, die
            die die aktuelle Prozessnummer (PID) enthält.
            <br />
            <br />
            Die Prozessnummer wird beim Schreiben einer Historiendatei für jeden
            Transfer
            protokolliert (siehe Parameter
            <paramref>history</paramref>
            ),
            um nachvollziehbar zu halten, von welchem Prozess Dateien übertragen wurden.
        </div>
    </note>
    <note language="en">
        <title />
        <div xmlns="http://www.w3.org/1999/xhtml">
            This parameter is used for Unix systems and - as opposed to other
            parameters -
            is usually specified in the start script
            <shell>sosftp.sh</shell>
            .
            The value of the environment variable
            <em>$$</em>
            is assigned, that
            contains the current process id (PID).
            <br />
            <br />
            The process id is used when writing an entry to a history file for
            each transfer
            (see parameter
            <paramref>history</paramref>
            ).
        </div>
    </note>
</param>