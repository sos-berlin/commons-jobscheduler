<?xml version="1.0" encoding="iso-8859-1"?>
<param xmlns="http://www.sos-berlin.com/schema/scheduler_job_documentation_v1.1"
       xmlns:xhtml="http://www.w3.org/1999/xhtml"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       name="classpath_base"
       required="false"
       DataType="SOSOptionFolderName">
        <note language="de">
		    <title/>
          <div xmlns="http://www.w3.org/1999/xhtml">
            Der Parameter wird bei der Installation des Programms auf einem Remote Server
            verwendet in Zusammenhang mit dem Parameter <paramref>operation</paramref>=install.
            Der Parameter bestimmt den Pfad des Java Runtime Environments (JRE)
            am Remote Server und wird eingesetzt falls auf diesem System
            kein JRE im Pfad verfügbar ist.
            <br/>
         <br/>
            Der Pfad des angegebenen JRE wird in das Start Script am Remote Server übernommen
            (<shell>sosftp.cmd</shell> bzw. <shell>sosftp.sh</shell>).
          </div>
        </note>
        <note language="en">
		    <title/>
          <div xmlns="http://www.w3.org/1999/xhtml">
            The parameter is used during installation of this program on a remote server
            with the parameter <paramref>operation</paramref>=installcode&gt;.
            This parameter specifies the path of the Java Runtime Environment (JRE)
            at the remote server and is used if on the remote server a JRE
            is not included in the system path.
            <br/>
         <br/>
            The path of the specified JRE is added to the start script at the remote server
            (<shell>sosftp.cmd</shell> respectively <shell>sosftp.sh</shell>).
          </div>
        </note>
      </param>